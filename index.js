/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './src/App';

// same value is used throughout the project, it should not change
const appKey = 'SelfAnalysisQuestionsApp';
AppRegistry.registerComponent(appKey, () => App);
